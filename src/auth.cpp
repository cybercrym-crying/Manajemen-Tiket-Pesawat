#include "../header/auth.h"
#include "../header/types.h"
#include <vector>
User *loginAccount(const vector<User> &user) {
  // input username and pass
  // cek ketersediaan akun dan status akun
  // jika username dan pass benar makan berhasil
  // maka return pointer user sekarang
  // jika gagal maka return nullptr
  return nullptr;
}
void registerAccount(vector<User> &user) {
  // input username and pass
  // cek apakah akun sudah ada yang punya
  // cek juga panjang pass kuat apa tidak
  // jika sudah tambahkan ke vector yang nantinya bakal manggil file handler
  // addUserFile buat nambah data buat tambahin akun ke account csv ide bagus
  // jika password di enkripsi
}
